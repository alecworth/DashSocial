<%- include('partials/header') -%>
<div class="container mt-5">
  <div class="row">
    <div class="col-6">
      <div class="p-4 mb-4 bg-light shadow-sm rounded" style="background-color: #c6c1a6;">
        <p><strong>User Name</strong>: <%= user.userName %></p>
        <p><strong>Email</strong>: <%= user.email %></p>
      </div>
      <div class="p-4 bg-light shadow-sm rounded" style="background-color: #c6c1a6;">
        <h2>Add a post</h2>
        <form action="/post/createPost" enctype="multipart/form-data" method="POST">
          <div class="mb-3">
            <label for="title" class="form-label">Title</label>
            <input type="text" class="form-control" id="title" name="title">
          </div>
          <div class="mb-3">
            <label for="caption" class="form-label">Caption</label>
            <textarea class="form-control" id="caption" name="caption"></textarea>
          </div>
          <div class="mb-3">
            <label for="imgUpload" class="form-label">Image</label>
            <input type="file" class="form-control" id="imgUpload" name="file">
          </div>
          <button type="submit" class="btn btn-primary" value="Upload">Submit</button>
        </form>
      </div>
    </div>
    <div class="col-6">
      <div class="p-4 bg-light shadow-sm rounded" style="background-color: #c6c1a6;">
        <ul class="row list-unstyled">
          <% for(var i=0; i<posts.length; i++) { %>
            <li class="col-6 mb-4">
              <a href="/post/<%= posts[i]._id %>">
                <img class="img-fluid rounded" src="<%= posts[i].image %>" alt="Post image">
              </a>
            </li>
          <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/feed">Return to Feed</a>
        </div>  
      </div>
    </div>
  </div>
</div>
<%- include('partials/footer') -%>